import React from 'react';
import PropTypes from 'prop-types';

const Blog = props => {
    return (
        <div>
            <h2>Blogs</h2>
        </div>
    );
};

Blog.propTypes = {
    
};

export default Blog;